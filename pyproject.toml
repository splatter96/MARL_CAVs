[project]
authors = [{name = "Paul Auerbach", email = "paul.auerbach@barkhauseninstitut.org"}]
dependencies = ["gymnasium==0.28.1", "numpy==1.24.4", "pandas==1.4.0", "parso==0.8.4", "pygame==2.5.0", "pyparsing==3.1.4", "stable-baselines3==2.3.2", "torch==2.4.1", "tqdm==4.67.1", "sb3-contrib @ file:///home/paul/Documents/PhD/RL/stable-baselines3-contrib", "tensorboard==2.13.0"]
name = "MARL_CAVS_pixi"
requires-python = "== 3.8.20"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
#marl_cavs_pixi = { path = ".", editable = true }

[tool.pixi.tasks]

[tool.pixi.dependencies]
wandb = ">=0.16.6,<0.17"
